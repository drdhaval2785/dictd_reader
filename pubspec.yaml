name: dictd_reader
description: A Dart package for reading DICTD dictionary files (.index and .dict/.dict.dz).
version: 0.1.2
repository: https://github.com/dhaval/dictd_reader

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  dictzip_reader: ^0.1.3

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
